#include<stdlib.h>
#include<stdio.h>
#include<stdbool.h>


// typedef struct Matcher{
// } Matcher;

bool pattern_matching(const char*, const char*);
void compile_result(const char*, const char*, bool);
